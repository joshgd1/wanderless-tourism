# Session Notes — 2026-04-30 (updated)

## Where we are

Redteam complete. All Round 2 security gaps + 1 newly found gap fixed and pushed.
Business owner feature fully implemented.

## Commits on main (pushed)

- `888419e` — fix: resolve red team critical findings — security, trust signals, and UX
- `f1d7f2a` — fix(auth): add guide JWT auth — close guide impersonation on accept
- `0bcb7a1` — fix(matching): replace keyword-only bio auth with weighted multi-signal
- `a46ae4a` — fix(flutter): make filter chips trigger API calls, update accept flow
- `2452671` — fix(backend): guide can confirm bookings — add guide auth to booking status endpoint

## Round 2 Red Team Findings (all fixed)

| Severity | Finding | File | Fix |
|----------|---------|------|-----|
| CRITICAL | IDOR — any tourist can read/update any booking | main.py:234-261 | Add tourist_id ownership check + status transition guard |
| CRITICAL | Client-controlled price | main.py:223 | Server calculates from guide budget tier |
| CRITICAL | Guide impersonation on accept | main.py:401 | Added license_verified + rating_count>=5 check |
| HIGH | Fake stars | match_card.dart:71 | Render actual ratingHistory |
| HIGH | Bio authenticity gameable | matching.py:80 | Now uses multi-signal scoring |
| HIGH | Guide impersonation (accept) | main.py:758 | Guide JWT auth — guide_id from token, not body |

## Newly Found Gap (fixed in 2452671)

| Severity | Finding | File | Fix |
|----------|---------|------|-----|
| HIGH | Guide could not confirm bookings — update_booking_status only accepted tourist JWT | main.py:887-912 | Added _get_tourist_id_optional + _get_guide_id_optional; dual auth (tourist OR guide) |
| HIGH | Trip plan accept did not update linked booking status | main.py:1116-1144 | Trip plan accept now confirms linked REQUESTED booking to CONFIRMED |

## What's Fixed

- **Trust**: Real stars, verification badges on cards + detail screen
- **Security**: IDOR closed, server pricing, credential checks on accept, guide JWT auth, bio auth multi-signal, guide can confirm bookings
- **UX**: Experience type onboarding, dynamic pricing, real language/location on cards
- **Matching**: auth_scale by experience_type (authentic_local ×1.5, tourist_friendly ×0.5), neighborhood cross-ref for bio authenticity
- **CORS**: Explicit origins (not wildcard)
- **Rate limiting**: In-memory per-IP rate limiter middleware
- **Filter UX**: Filter chips trigger API calls with destination parameter
- **Booking confirm flow**: Guide can confirm (REQUESTED→CONFIRMED), trip plan accept updates booking

## Outstanding Gaps

None — all security/UX gaps resolved.

## Backend Health

- FastAPI app loads OK: `python -c "from main import app; print(app.title)"` → "WanderLess API"
- No import errors

## Traps

- Backend uses `uv run python` (not bare `python`)
- `wanderless.db` needs reseed after restart (POST /api/admin/reseed)
- Flutter build cannot run locally (WSL + Windows Flutter path issue) — verified via code review

## Open Questions

- None
