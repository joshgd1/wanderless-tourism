# Session Notes — 2026-05-04 (evening)

## Where we are

Group tours feature fully deployed. Render deploy failure root-caused and fixed: Flutter compilation errors (4 files) blocked the CI pipeline before the Render deploy step. Flutter Web + APK builds now passing. `/codify` complete — flutter-api-patterns skill created.

## Read first

1. `.claude/accounts/term-536/plans/velvety-stirring-neumann.md` — Group Tours plan (what was built vs. what remains)
2. `.claude/skills/19-flutter-patterns/flutter-api-patterns.md` — Flutter API/auth patterns skill (192 lines, created this session)
3. `journal/0009-DISCOVERY-render-deploy-pipeline-blocking.md` — why Flutter errors blocked Render deploy
4. `journal/0009-DECISION-flutter-api-patterns-skill.md` — what was codified
5. `.claude/.proposals/latest.yaml` — proposal for upstream flutter-api-patterns skill

## In-flight state

- Render pipeline: healthy (flutter-web.yml + flutter-apk.yml both passing as of 12:24 UTC)
- `/codify` step 4 (SKILL.md index) and step 7 (proposal) done — steps 5-6 (README, red team) skipped for a 1-skill addition
- 4 Flutter compilation errors fixed this session: guide_jobs_screen, guide_detail_screen, confirm_request_screen, trip_plan_list_screen
- Proposal at `.claude/.proposals/latest.yaml` — pending upstream review

## Traps

- **Coherence label index (UNVERIFIED)**: `form_groups` uses `grp[0].get("_group_tags", [""])[-1]` — depends on internal mutation sort order in `_compute_group_coherence()`. Not verified against ML engine source.
- **Flutter SDK missing on WSL2**: `flutter analyze` never run locally — Dart correctness only verified via GitHub Actions CI.
