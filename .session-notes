# Session Notes — 2026-05-17

## Where we are

Final compressed report `FINAL_GROUP_ASSIGNMENT_REPORT.md` created and pushed (~2,465 words, 7 pages). README updated to link only to the new report. Old `FINAL_GROUP_ASSIGNMENT_REPORT_8P.md` deleted. Repo is clean.

## Read first

1. `FINAL_GROUP_ASSIGNMENT_REPORT.md` — the submitted 7-page report for Prof. Jack Hong
2. `README.md` — links to the final report; verify "Final Report" section points to `FINAL_GROUP_ASSIGNMENT_REPORT.md`

## In-flight state

None. All changes committed and pushed.

## Traps

- Git push always rejected (remote has new commits). Always `git pull --rebase origin main && git push origin main`.
- The formatter hook modifies files on write — if a targeted old_string no longer matches, re-read the file first.
