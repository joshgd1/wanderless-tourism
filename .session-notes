# Session Notes — 2026-05-07

## Where we are

Pitch deck red team complete. 16-slide WanderLess investor deck red-teamed across market sizing, unit economics, ML framing, safety claims, competitive moat, and team capacity. 10 findings: 1 CRITICAL (SAM math off by 3 orders), 3 HIGH, 6 MEDIUM.

## Read first

1. `journal/` — existing decisions and risks for this project; check for prior WanderLess red team entries
2. `backend/ml/recommender.py` — ML DESTINATIONS dict; verify image_url/tag pairs match before editing
3. `app/lib/features/discover/screens/discover_screen.dart` — Flutter destination cards; verify imageUrl matches `tag` field

## In-flight state

- Pitch deck red team findings NOT yet journaled — should be saved to `journal/` if the human wants to track them
- Landmark image fix (Kuala Lumpur: Petronas Twin Towers, Bali: Tegallalang Rice Terrace) was completed in prior session and deployed

## Traps

- SAM math error on Slide 3: "3M SG trips × 20% × $25 = $15M" but deck states "$180B" — this will destroy credibility with any investor who asks
- 25% guide attach rate in unit economics is unrealistic for MVP; realistic is 3-5%
- "Verified" and "safety-first" are never defined as product features — investor Q&A will probe this

## Open questions for the human

- Should the pitch deck red team findings be saved to `journal/` as a DISCOVERY entry?
- Is there additional WanderLess project work to do in this workspace, or is the session complete?
