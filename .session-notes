# Session Notes — 2026-05-04

## Where we are

Building the AI Safety Score feature (trip plan → ML-computed safety score → color badge display). Backend ML engine and API endpoint are done. Flutter model, API client method, card widget, and integration into create_trip_plan_screen are all done. Last fix was a Dio error on "Send Request" — guide_id was sent as JSON body but backend expects query parameter.

## Read first

1. `backend/ml/safety_score.py` — ML scoring engine (Area×0.30 + Route×0.20 + Time×0.15 + Transport×0.10 + Weather×0.10 + Venue×0.10 + TravellerFit×0.05)
2. `backend/main.py` line 2236 — `POST /api/safety/score` endpoint
3. `app/lib/features/trip_plan/screens/create_trip_plan_screen.dart` — SafetyScoreCard dialog after plan creation (lines ~185-207)
4. `app/lib/shared/widgets/safety_score_card.dart` — display card widget
5. `app/lib/shared/models/safety_result.dart` — Flutter data model
6. `app/lib/core/api_client.dart` line 217 — `requestGuideFromPlan` fix: `queryParameters` instead of `data` for guide_id

## In-flight state

- Safety Score feature fully built but uncommitted
- Dio error on guide request fixed (was data→queryParameters)

## Traps

- Flutter analyze unavailable (WSL dart tool path broken); verify with `flutter build web`
- Backend `uv run python` must be run from `backend/` directory
- Render backend free tier is suspended — check `dashboard.render.com` for manual redeploy

## Open questions for the human

Was the Safety Score dialog appearing after creating a plan? Any other Dio errors to fix?
