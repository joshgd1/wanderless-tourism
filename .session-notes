# Session Notes — 2026-05-05

## Where we are

Fixed Flutter Web build failure: `_syntheticGroups` in `groups_screen.dart` used non-existent `TravelGroup` fields (`tripDate`, `durationDays`, `interests`, `memberProfiles`). Replaced with correct model fields (`proposedDate`, `proposedDuration`, `members`/`TravelGroupMember`). Push `01281a9` is green for Deploy Flutter Web.

## Read first

1. `app/lib/features/groups/screens/groups_screen.dart` — fixed synthetic data; uses `TravelGroupMember` for members
2. `app/lib/features/guide/screens/guide_dashboard_screen.dart` — has `openRequestsProvider` + `_OpenRequestCard` added prior session; card lacks Accept/Decline buttons
3. `app/lib/features/guide/screens/guide_login_screen.dart` — beautified with hero image + social login
4. `app/lib/features/trip_plan/screens/create_trip_plan_screen.dart` — synthetic safety score fallback (87)

## In-flight state

- `_OpenRequestCard` on guide dashboard displays open requests but has no Accept/Decline action buttons
- Guide login and tourist login are now very similar in visual style (hero image, social buttons)
- Flutter APK builds were running at session end — check `gh run list` for status

## Traps

- `openRequestsProvider` and `_syntheticOpenRequests` are defined AFTER their reference sites in `guide_dashboard_screen.dart` — Dart top-to-bottom compilation works but ordering is fragile

## Open questions for the human

- Should `_OpenRequestCard` have Accept/Decline buttons like booking cards do?
- Was the intent to make guide login and tourist login nearly identical, or improved-but-distinct?
